# BestERP Phase 0a Spike — Environment

# Application role (non-superuser, subject to RLS)
# IMPORTANT: RLS does NOT work with superuser roles!
DATABASE_URL="postgresql://besterp_app:CHANGEME@localhost:5434/besterp"

# Admin/migration role (superuser, bypasses RLS for migrations)
DATABASE_ADMIN_URL="postgresql://besterp:CHANGEME@localhost:5434/besterp"
